# v0.1.1 配布前の実操作確認

## Auto Fixの確認

- [ ] 別マテリアル・別オブジェクトの同じ項目が同じ値へ変更されても、Auto Fixの各詳細にその項目自身の場所だけが表示される
- [ ] Auto Fixの詳細を複数ページ切り替えても項目・値・場所が欠けず、通常の共有マテリアル変更では従来どおり共有先をまとめて表示する
- [ ] 更新後に変更のない状態で起動してもキー生成の差による追加・削除ログが出ず、その後の変更は正常に記録される
- [ ] SDK検証・Auto Fixの前後でanimationHashSetのhash・name・配列サイズが個別ログに出ず、これらだけの更新で保存が増えない
- [ ] 既存履歴を読み直すとanimationHashSetの行が表示・検索・TXTから除外され、除外項目の削除ログも発生しない。Auto Fixの実変更と通常設定の履歴は残る
- [ ] SDKとACLで同じアバターを指定し、SDKのAuto Fixを1回押すとログが1行にまとまり、詳細に各分類・場所・変更前後が表示される
- [ ] Auto Fixの直前に手作業で変更した項目は別のログに残り、Auto Fixに混ざらない
- [ ] Auto Fixを続けて2回押すと各実行で1行ずつ記録され、変更がない場合は保存が増えない
- [ ] 詳細の項目名・子の名前・値で検索でき、分類絞り込みとTXT出力でも内訳を確認できる
- [ ] Auto Fix後の通常操作は通常のログになり、一時停止中・SDKで別アバターを選択した場合は誤って集約されない
- [ ] Auto Fixボタン外へのドラッグでクリックを中止した場合や、無効なボタンでは誤って集約されない。キーボード操作でも確認する
- [ ] SDKパネルを閉じて開き直した後、ドッキング変更後、Unity再起動後も実行を認識する
- [ ] 途中でインポートが入る修正でも不要な中間保存が増えず、修正後に通常記録へ戻る

## 基本機能

- [ ] 新規Unity 2022.3.22f1プロジェクトに導入し、Consoleにコンパイルエラーがない
- [ ] 保存済みシーンのアバターを指定し、BlendShapeの変更前後の値が記録される
- [ ] lilToonの数値・色・テクスチャ・UV変更を記録し、項目名が読みやすい
- [ ] 共有マテリアルの変更が1件にまとまり、各参照先が詳細に出る
- [ ] 衣装追加・削除が親1件にまとまり、子とテクスチャを含む参照アセットが詳細に出る
- [ ] コンポーネントの追加・削除・設定変更が記録される
- [ ] 通常のHierarchy選択が勝手に変わらない
- [ ] 入力・検索ボタン・Enterによる検索と分類が機能し、子の名前でも検索できる
- [ ] Undo/Redo、ウィンドウを閉じた状態、Unity再起動後でも記録できる
- [ ] 再生中の一時的な変化を記録しない
- [ ] 最新100状態に保存上限が固定され、超過後も表示・検索・TXT出力が正常
- [ ] 保存・整理失敗が表示され、問題解消後に通知が解除される

自動テスト結果はvalidation-results.txt。Unityでの上記実操作確認は未実施です。

- [ ] 横長C案のロゴが縦横比を保って幅に追従し、バージョン表記と重ならない。ライト・ダーク両テーマで文字が読める

- [ ] 最後の保存が失敗した際、アバター切り替えが中断されてエラーが表示される
- [ ] 履歴整理の失敗を解消した後、新しい変更がなくても画面上の古い履歴が除去される

- [ ] lilToonのカラー1回の変更に伴う同じ記録内の項目が1行になり、詳細に各変更前後の値が出る
- [ ] 同じマテリアルの共有先が重複項目にならず、別マテリアルの変更は別行になる
- [ ] 集約した詳細項目を検索でき、TXT出力にも内訳が含まれる

## 表示・記録の確認

- [ ] 同じシェーダーを使う別マテリアルの値が個別に記録され、共通の項目名が正しく表示される
- [ ] シェーダーの切り替え・再インポート・includeファイル変更後も値を記録できる。項目数を維持したID・型変更も確認する
- [ ] マテリアルの参照解除・記録の一時停止で、不要なシェーダー情報が残り続けない
- [ ] 衣装の大量追加・削除が従来どおり親単位でまとまり、子オブジェクト・参照アセットの詳細が欠けない
- [ ] 詳細の項目名を検索した後、別の項目名・整形済み数値・存在しない語へ切り替えても検索結果が正しい

- [ ] 名前検索後に詳細内の別キーワードへ切り替えても、検索漏れがない。多数のログでも複数語・全角文字・詳細の検索が一致する
- [ ] 別アバターの位置・コンポーネント値に関するUndo変更通知では取得を予約せず、対象アバター自身のUndo/Redo・共有マテリアル・Prefabアセットの変更は記録される（定期確認・階層変更通知による取得は継続）
- [ ] 100状態を超えた整理後も画面が更新され、ファイルがロックされて整理に失敗した場合も、残った履歴が表示される

- [ ] 文字を続けて入力・削除しても検索結果が即座に更新され、詳細の値・全角英数字・複数語の検索結果が変わらない
- [ ] 検索中に新しいログが追加されても結果に反映され、上限整理された古いログや別アバターのログが残らない
- [ ] 詳細を開閉・ページ移動・再読込しても項目名と参照先が正しく、表示の取り違えがない
- [ ] 日付の文字・矢印をクリックすると、その日のログと列見出しだけが折りたたまれ、再クリックで戻る
- [ ] 同じ日付が複数ページにまたがっても状態を維持し、別の日付は独立して開閉できる
- [ ] 折りたたみ中の自動記録・再読込で勝手に展開されず、検索条件変更・対象切り替えで展開に戻る
- [ ] 「最新へ」で最新ログの日付が展開される。折りたたみ中も検索件数・TXT出力内容は変わらない
- [ ] 全分類の項目名が太字、その下に変更前・変更後または要約が並び、最小幅とライト／ダークテーマで文字・色見本が読める
- [ ] 長い項目名・値が列内に折り返され、色見本の不透明度が百分率で表示される
- [ ] TXTのマテリアル欄が項目単位に改行され、省略項目の詳細IDが正しい
- [ ] マテリアルの色を変更すると、変更前後の色見本と項目名が表示される（導入後の記録同士で確認）
- [ ] 色見本にマウスを重ねるとRGBA値が出る。透明色・HDR値の注記と表示を確認
- [ ] 数値変更が「1.00 → 0.70」のように表示され、「0.70」で検索できる
- [ ] 同じマテリアルの複数項目を同時変更すると代表1項目と「ほか○項目」を表示し、詳細には全項目が残る
- [ ] マテリアルファイルの同時更新は詳細に「保存情報の更新あり」と表示し、ほか○項目の件数には含めない
- [ ] シェイプキー・位置・スケール・有効状態の前後が読める。回転の元の値は詳細に出る
- [ ] オブジェクト・コンポーネントの追加削除が「なし／あり」で分かり、子の内訳・コンポーネント初期設定は詳細に出る
- [ ] 対象欄は名前と分類だけを表示し、同名の別オブジェクトは詳細の階層パスで区別できる
- [ ] 長文・内部設定・識別値は一覧を圧迫せず、詳細で元の値を読めてコピーできる
- [ ] 同名の参照先への差し替えも変更があったことを表示し、詳細で参照先を区別できる
- [ ] 詳細へ移した値・参照先・項目名も検索でき、TXTの詳細に元の値が残る
- [ ] TXTの変更内容に項目名・値が入り、色はRGBA値で出力される。省略した項目は詳細で確認できる
- [ ] 既存履歴も読み込め、色の種別情報がない値は数値で表示される
- [ ] 最小幅と広いウィンドウの両方で列がそろい、長い名前・値が折り返される
- [ ] マテリアルと子階層の詳細、検索、40件を超えるページ送りが正常
- [ ] TXTの一覧IDと詳細IDが対応し、変更前後の値・参照先が残る

## アバター欄とログ表示

- [ ] アバター名と「記録中／一時停止中」が表示され、開始前・Unity処理中・エラーを区別できる
- [ ] 一時停止時に直前の変更は保存され、停止後の操作では新しい履歴が増えない
- [ ] 停止中に値を10から80へ変更し、再開後に90へ変更すると「80→90」だけがログ・検索・TXTに出る
- [ ] Aを停止してBへ切り替えるとBは記録でき、Aへ戻ると停止したままになる（同名の別アバターでも確認）
- [ ] AとBの両方を停止した後、Aだけ再開してもBの停止設定は維持される
- [ ] ウィンドウを閉じる・Unityを再起動する・シーンを開き直す操作で停止状態が維持される
- [ ] 停止中にシーンを別名保存しても、同じ指定対象が勝手に記録を再開しない
- [ ] 保存失敗時に一時停止／再開が成功したように表示されず、エラーと実際の状態を確認できる
- [ ] アバター欄を空にするとログ一覧・記録件数がクリアされる
- [ ] 対象を削除、または対象シーンを閉じた際に古いログが残らない
- [ ] 空欄で再読込を押しても過去ログが出ず、TXT出力が無効になる
- [ ] 対象を再指定、またはシーンを再度開くと保存済みログが戻る
