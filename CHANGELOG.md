# GitHub公開（2026-09-23／v0.1.1）

- タイトルロゴをC案（白いACL・薄紫の区切り線・Avatar Change Log）に変更。
- 画像の余白を整理し、縦横比を保ってウィンドウ幅に追従するよう調整。
- バージョン表記は v0.1.1 を維持。

# v0.1.1 — 更新版（2026-09-22）

- Auto Fix詳細で別マテリアル・別オブジェクトの同じ項目が同じ値へ変更された際、参照先が混在する表示を修正。各詳細にその変更自身の場所を表示。
- Auto Fix詳細のページ表示で全項目の参照先対応表を作らず、一覧でも不要な子項目の抽出・並べ替えを省略。通常の共有マテリアル表示は維持。
- 項目キーの作成に取得処理内で再利用するStringBuilderを使用し、中間配列・文字列を削減。識別キーと保存形式、記録間隔、保存上限は維持。

- SDK検証で再生成されるVRCAvatarDescriptorのanimationHashSetを記録対象外に変更。Auto Fix前後に大量の内部設定ログが別行で並ぶ問題を修正。既存履歴と再開基準も読み込み時に除外し、削除ログの発生を防止。保存済みJSONは変更しない。

- SDKのAuto Fixボタン1回による変更を一覧1行に集約し、分類・項目・場所・変更前後を詳細に表示。検索・分類フィルター・TXT出力に対応。
- Auto Fix実行中の中間保存を抑え、終了後の状態だけを保存。実行直前の手作業は別件として保存し、変更のない実行は保存を省略。

- ログ一覧の「変更後」の値を水色から白文字に変更。

- 記録中のシェーダー項目ID・型をシェーダーごとに再利用。アセット再インポート時に再取得し、参照解除・記録停止時に解放。値の監視間隔は維持。
- スナップショット取得内でコンポーネントの番号付き項目名とシェーダー表示名を共有し、Rendererのマテリアル取得に作業用リストを再利用。
- オブジェクト追加・削除の親を階層パスから解決。同一場所のコンポーネントとマテリアル参照先も対応表で検索し、大量変更時の繰り返し走査を削減。
- 詳細検索は元の項目名・値を先に判定し、必要な場合だけ表示用文字列を生成。途中までの検索データも上限内で再利用し、別の検索語で未確認の項目が必要になった場合は再取得。

- 検索は名前・概要から順に判定し、一致時点で終了。キャッシュ上限を超えたログでも詳細全文の先行生成を避け、詳細・複数語・全角文字の検索を維持。
- 対象アバターの階層外にあるシーンオブジェクトのUndo変更通知では再確認を予約しない。共有アセット・Prefabアセット・判定できない変更通知は引き続き監視。
- 履歴整理で得た残存ファイル一覧をログ画面へ渡し、通常の保存時に行っていたファイル存在確認の重複を削減。整理失敗時はディスクから表示を照合。

- 旧表示用の未使用データと呼び出されない補助処理を削除。一時停止からの再開基準に対する不要な差分生成を省略。
- 検索用データを上限付きで再利用し、検索解除・再読込・対象切り替え・ウィンドウ終了・履歴整理に合わせて解放。
- 詳細の項目名・参照先をページ単位で再利用。再描画ごとの抽出・重複除去・文字列生成を削減。
- 「最新へ」の日付取得と、値の改行判定に伴う不要なデータ生成を削減。

- 対象アバター名・自動記録状態の表示と、アバター欄横の一時停止／再開ボタンを追加。
- 停止状態をアバターIDごとに保存。停止中は変更監視と履歴保存を行わず、再開時に新しい比較基準を保存し、停止中の変更をログから除外。
- ウィンドウを閉じる・対象を切り替える・Unityを再起動する操作でも停止状態を維持。

- 日付見出しのクリックでログを展開・折りたたみ可能に。検索・TXT出力・記録処理は維持。

- 全分類の項目名と変更前後を上下に分離し、変更後の値を強調。位置・スケールの軸、有効／無効、追加削除の有無を読みやすく表記。
- 色見本を拡大し、不透明度を百分率で表示。TXTも全分類を共通の形式に変更し、省略項目には対応する詳細IDを表示。

- マテリアルの代表的な変更1項目を一覧に表示し、残りは「ほか○項目」と表示。内部設定・保存情報・共有先は詳細に集約。
- 長い値・識別値・回転の内部数値・階層パスは詳細に集約。省略した値と参照先も詳細・検索で確認可能。
- 表示用に整形した数値も検索対象に追加。色の種別情報がない既存履歴は文字表示を継続。

- 画面とTXTの時刻表示を分まで（HH:mm）に統一。保存時刻の秒以下の精度は維持。

- アバター欄が空のときに保存IDだけで過去ログが表示される問題を修正。
- シーン未読込・対象削除・選択解除時は表示をクリアし、対象復帰時は履歴を再表示。
- 空欄中の保存通知とTXT出力を抑止。保存済みログと記録対象の復帰設定は維持。

- ログ一覧をC案の時刻・操作、対象・分類、変更内容の列表示に変更。
- 詳細の開閉、検索、ページ送り、マテリアル・階層の集約を維持。
- TXTをID付きの一覧と詳細に分離し、全ページ分を出力。


# v0.1.0 — 初リリース

- アバターの改変履歴を自動記録。ウィンドウを閉じても指定対象の記録を継続。
- オブジェクト、Transform、シェイプキー、マテリアル、コンポーネントの変更を表示。
- 階層追加・削除を親単位に集約。同じ記録内の同一マテリアルの変更を1行に集約。
- 詳細表示、分類・文字検索、変更ログのTXT出力。
- アバターごとに最新100状態をローカル保存。
- 紫のグラデーションを使用した横長ACLロゴを採用。

開発中の版番号を整理し、最初の配布版をv0.1.0に統一しました。記録形式と機能は変更していません。
